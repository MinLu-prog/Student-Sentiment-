/**
 * Artwork shared by the full-bleed hero headers.
 *
 * Kept in one place on purpose: these paths were previously inlined into five
 * screens, so foldering `main2.jpg` under `CarParking/` silently 404'd every
 * one of them and the heroes fell back to flat navy.
 */

/** MIIT main building — the default campus hero photo. */
export const HERO_IMAGE = '/campus/CarParking/main2.jpg'

/** Aerial campus shot used by the sentiment dashboard hero. */
export const SENTIMENT_HERO_IMAGE = '/campus/topviewjpg.jpg'
